{
  "_comment": "pi-mcp-adapter читает этот файл по пути ~/.pi/agent/mcp.json. install.sh мерджит ТОЛЬКО ключи settings и mcpServers — пользовательские сервера сохраняются. Пакет opus-pack-pi по умолчанию MCP-серверы не регистрирует; добавляй свои в mcpServers ниже.",

  "settings": {
    "toolPrefix": "server",
    "idleTimeout": 10
  },

  "mcpServers": {}
}
