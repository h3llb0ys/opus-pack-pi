{
  "_comment": "pi-mcp-adapter reads this file from ~/.pi/agent/mcp.json. install.sh merges ONLY the settings and mcpServers keys — user-added servers are preserved. opus-pack-pi registers no MCP servers by default; add your own under mcpServers below.",

  "settings": {
    "toolPrefix": "server",
    "idleTimeout": 10
  },

  "mcpServers": {}
}
